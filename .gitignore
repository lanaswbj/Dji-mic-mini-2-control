# Rust
/target
**/*.rs.bk
.cargo-home/

# Node / frontend
node_modules/
gui/dist/
.npm-cache/

# Tauri generated
gui/src-tauri/gen/
gui/src-tauri/target/
native/**/build/

# Local environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
# Git Bash crash dumps — they land in whatever directory a `bash.exe` happened
# to die in. See the root CLAUDE.md on running MSVC builds through PowerShell
# rather than Git Bash.
*.stackdump

# Editor / IDE
.idea/
.vscode/
*.iml
*.swp

# OS
.DS_Store
Thumbs.db

# Release artifacts (produced by build-release.sh / .ps1)
/Release
/outputs
/work
